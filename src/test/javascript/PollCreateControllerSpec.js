describe("PollCreateController", function() {
    var controller, httpBackend;

    beforeEach(module('dot-voting'));

    beforeEach(inject(function($controller) {
        controller = $controller('PollCreateController', {});
    }));

    describe("", function() {
        it("", function() {

        });
    });
});